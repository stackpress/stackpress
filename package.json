{
  "private": true,
  "workspaces": [
    "examples/*",
    "stackpress"
  ],
  "scripts": {
    "build": "yarn --cwd stackpress build",
    "fs": "yarn --cwd examples/with-fieldsets",
    "min": "yarn --cwd examples/with-minimal",
    "uno": "yarn --cwd examples/with-unocss",
    "cjs": "yarn --cwd examples/with-commonjs"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}