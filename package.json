{
  "private": true,
  "workspaces": [
    "templates/*",
    "stackpress"
  ],
  "scripts": {
    "build": "yarn --cwd stackpress build",
    "test": "yarn --cwd stackpress test",
    "blog": "yarn --cwd templates/blog",
    "store": "yarn --cwd templates/store",
    "website": "yarn --cwd templates/website"
  }
}