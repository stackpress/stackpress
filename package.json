{
  "private": true,
  "workspaces": [
    "templates/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn build:core && yarn build:util && yarn build:view && yarn build:main",
    "build:core": "yarn server build && yarn schema build && yarn sql build",
    "build:main": "yarn api build && yarn stackpress build",
    "build:util": "yarn csrf build && yarn language build && yarn email build",
    "build:view": "yarn view build && yarn session build && yarn admin build",
    "admin": "yarn --cwd packages/stackpress-admin",
    "api": "yarn --cwd packages/stackpress-api",
    "csrf": "yarn --cwd packages/stackpress-csrf",
    "email": "yarn --cwd packages/stackpress-email",
    "language": "yarn --cwd packages/stackpress-language",
    "schema": "yarn --cwd packages/stackpress-schema",
    "server": "yarn --cwd packages/stackpress-server",
    "session": "yarn --cwd packages/stackpress-session",
    "sql": "yarn --cwd packages/stackpress-sql",
    "stackpress": "yarn --cwd packages/stackpress",
    "view": "yarn --cwd packages/stackpress-view",
    "blog": "yarn --cwd templates/blog",
    "store": "yarn --cwd templates/store",
    "website": "yarn --cwd templates/website"
  }
}